# ============================================================
# McCulloch Website — .gitignore
# ============================================================

# Local archive (backup/junk files — stays on your machine only)
_archive/

# Dependencies
node_modules/
*/node_modules/

# Logs
*.log
logs/
npm-debug.log*

# Build outputs
# dist/ committed for VPS deployment
build/
out/

# Environment variables (keep .env.example, never commit real .env)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
Server/.env.production

# IDE / OS files
.vscode/
.idea/
.DS_Store
.DS_Store?
Thumbs.db
*.swp
*.swo
*~

# Temporary / backup files
*.tmp
*.temp
*.old
*.new
nul
Server/nul

# Large media files (stored in Docker volumes on VPS, not in git)
*.mp4
*.mov
*.avi
*.mkv
*.webm

# ...but the site's own public assets ARE needed at build time (the frontend
# container rebuilds from source and copies Client/public into dist), so keep them.
!Client/public/videos/
!Client/public/videos/*.mp4
!Client/public/videos/*.webm

# Server uploads — stored in Docker volume, never in git
Server/uploads/

# Scraper data — large JSON + images, local use only
Server/scripts/*/output/
Server/scripts/festina-scraper/
Server/scripts/roamer-scraper/
Server/scripts/briston-scraper/output/

# Source images — large PNGs, stored on VPS only
Client/images/
